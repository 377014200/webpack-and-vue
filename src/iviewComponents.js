import { Button, Table, Panel, Collapse } from 'iview';

export default function ( Vue ) {

   Vue.component( 'Button', Button );
   Vue.component( 'Table', Table );
   Vue.component( 'Panel', Panel );
   Vue.component( 'Collapse', Collapse );

}